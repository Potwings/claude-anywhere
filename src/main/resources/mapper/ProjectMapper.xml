<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.yonggeon.claudebot.mapper.ProjectMapper">

    <resultMap id="ProjectResultMap" type="Project">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="workingDirectory" column="working_directory"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="ProjectResultMap">
        SELECT id, user_id, name, description, working_directory, status, created_at, updated_at
        FROM project
        WHERE id = #{id}
    </select>

    <select id="findByUserIdAndName" resultMap="ProjectResultMap">
        SELECT id, user_id, name, description, working_directory, status, created_at, updated_at
        FROM project
        WHERE user_id = #{userId} AND name = #{name} AND status != 'DELETED'
    </select>

    <select id="findByUserId" resultMap="ProjectResultMap">
        SELECT id, user_id, name, description, working_directory, status, created_at, updated_at
        FROM project
        WHERE user_id = #{userId} AND status != 'DELETED'
        ORDER BY updated_at DESC
    </select>

    <select id="findByUserIdAndStatus" resultMap="ProjectResultMap">
        SELECT id, user_id, name, description, working_directory, status, created_at, updated_at
        FROM project
        WHERE user_id = #{userId} AND status = #{status}
        ORDER BY updated_at DESC
    </select>

    <insert id="insert" parameterType="Project" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project (user_id, name, description, working_directory, status)
        VALUES (#{userId}, #{name}, #{description}, #{workingDirectory}, #{status})
    </insert>

    <update id="update" parameterType="Project">
        UPDATE project
        SET name = #{name},
            description = #{description},
            working_directory = #{workingDirectory},
            status = #{status}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE project
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM project
        WHERE id = #{id}
    </delete>

</mapper>
